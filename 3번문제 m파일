function A = kms_24013976_func_3(r)
% 3-(b) 문제: R을 유일한 입력 변수로 받아 표면적 A를 계산하는 함수
% 3-(a) 에서 유도한 식을 바탕으로 계산합니다.

    global V; % V를 함수 내에서 전역 변수로 선언 (지시사항 충족)
    
    % h가 소거된 표면적 수식 적용
    A = sqrt((pi^2 * r.^4) + (9 * V^2 ./ r.^2)); 
end
